"""Claude Code hook scripts shipped with the package.

These are *not* importable hooks — they're standalone scripts Claude Code
invokes directly. They live inside the package so ``importlib.resources``
can resolve them in both editable and wheel installs.
"""
